# Synapse Architecture & Coding Standards

## System Invariants & Authoritative Boundaries
- System Principle: Core decides, Maestro delivers.
- Authority Model: CLAUDE.md is the immutable constitution. Do not alter architecture, add NLP/prose generation inside Core, or introduce probabilistic logic on your own initiative.
- Pipeline Boundaries: Constitutional Airlock (Ingestion) -> Constitutional Adjudication Engine (Core) -> Evidence Emitter (Audit).
- Fail-Closed Doctrine: Reject unstructured prose or invalid schemas immediately with HTTP 422 / Schema Error. Zero internal NLP/prose parsing inside Core.
- Core Adjudication Constraints: Stateless, pure functions, zero-generation logic executing deterministic rule checks.

## Locked Design Principles
- Supervisor UI Principle: No GO/NO-GO surface may present a standalone verdict. Every decision shown to a user must be paired with the Rule ID and the full set of evaluated sub-conditions (pass/fail per condition); for NO-GO, the specific conflicting condition must be shown explicitly. Applies to every delivery surface (dashboard, WhatsApp, Telegram, future channels).
- Hybrid Surface Design: Every delivery channel supports two directions — inbound (a requester queries the status of a claim) and outbound (Synapse pushes a GO/NO-GO alert unprompted). A channel adapter is incomplete unless it implements both.
- Escalation Requirement: Every rendered GO/NO-GO alert must state how to escalate — who or what contact point to reach — regardless of channel. Maestro and its channel adapters only ever display this contact information; they never execute an action on the adjudication themselves. **Amended 5 Aug 2026 (Supervisor Override Retirement, below):** originally worded as "a contact point to reach for an override"; override is now retired entirely, so that framing is stale and is corrected here rather than left to mislead a future reader. The contact point now names a resolved authority, not an override path.
- Admin-Override Evidence Principle: An admin override never mutates, replaces, or deletes an existing adjudication record — Core is stateless and re-evaluates from live PostgreSQL/Redis state rather than storing a mutable verdict, so there is no stored decision to flip. An override is validated (`src/supervisor/schemas.py`'s `OverrideRecord`: `extra="forbid"`, mandatory non-empty `justification`), authorized by pure, zero-I/O logic (`src/supervisor/logic.py`'s `evaluate_override`, mirroring `core/rules.py`'s pattern: fails closed if the issuer is unauthenticated, and fails closed if the claim being overridden has no existing adjudication record), and — only if accepted — emitted as its own distinct, SHA-256-signed `OverrideRecord` evidence entry (`evidence/emitter.py`'s `emit_override_evidence`), additive alongside the original `AdjudicationRecord`, linked only by `claim_id`. Core's determinism is preserved: the same claim always adjudicates the same way; an override is a separate, layered, human fact, never a rewritten decision. Exposed live at `POST /supervisor/override` (`src/supervisor/router.py`): 403 if the issuer is unauthenticated, 404 if the claim has no adjudication record on file (unlike `/airlock/claims`, where NO_GO is a valid outcome and always 200 — a rejected *override* means the administrative action didn't happen at all, so it uses real HTTP status semantics instead). Both the original adjudication and the override are now genuinely persisted, append-only, in PostgreSQL — `src/evidence/models.py`/`repository.py` (`adjudication_records`, written by `src/airlock/router.py` right after `emit_evidence()`) and `src/supervisor/models.py`/`repository.py` (`override_records`, written only when an override is accepted, never for rejections). Neither table uses `claim_id` as a primary key: these are audit logs, not latest-state tables, so multiple entries per claim are permitted by design.
- Admin-Override Notification Principle: An accepted override triggers a Maestro alert. Reasoning: an override is exactly the kind of event someone other than the overriding supervisor should hear about — a human just intervened in a fail-closed system, and staying silent about that would undercut the whole point of an audit trail. The alert is built from the existing `OutboundAlert` schema (decision `GO`, a single `rule_trace` entry describing the override, `escalation_contact` naming the overriding issuer) and sent through the existing `WhatsAppAdapter`/`TelegramAdapter` stubs — no new Maestro surface added. This is the *only* place `src/supervisor/` imports `src/maestro/`: `src/supervisor/logic.py`'s `evaluate_override` stays fully decoupled (zero Maestro imports), only `src/supervisor/router.py` (the orchestration layer) wires the two together, mirroring how `src/airlock/router.py` wires Core and Evidence without either depending on the router. This principle covers *override* notifications only.
- NO_GO Notification Principle: **DECIDED 2026-07-31.** A NO_GO adjudication from `POST /airlock/claims` triggers a Maestro alert, for every failure class (`R-PTW-01`, `R-AUTH-01`, `R-ZONE-01`) — not just overrides. GO adjudications deliberately do not trigger one; that asymmetry is intentional, not an oversight. Follows the exact same pattern as the Admin-Override Notification Principle above: `src/airlock/router.py` builds an `OutboundAlert` (via `OutboundAlert.from_evidence_record()`, which now also carries `reason_code` straight from the evidence record — see Reason Code Convention) and hands it to both `WhatsAppAdapter` and `TelegramAdapter` unconditionally, mirroring how `src/supervisor/router.py` is the only place that touches both Core/Evidence and Maestro. `recipient_id`/`escalation_contact` are no longer sourced from `claim.issuer_id`/placeholder text — see Escalation Ownership Principle, below, for the current (2026-07-31, superseding) mechanism. This does not apply to the override flow's `escalation_contact`, which already names the real overriding issuer and remains untouched. Unlike the override endpoint, `/airlock/claims`'s response shape is unchanged — Maestro notification results are not added to the HTTP response; delivery stays a side effect, kept out of the adjudication response contract deliberately, to avoid changing an API shape nothing asked to change.
- Escalation Ownership Principle: **DECIDED 2026-07-31.** Who owns escalating a rejected claim is determined by the adjudicated failure reason (`reason_code`), not by the work activity that was attempted (`claim_type`/`work_type`) — a PTW failure and an authority failure in the same zone are two different people's problem. Resolved deterministically by `src/maestro/directory.py`'s `resolve_authority(zone_id, reason_code) -> AuthorityBinding` (`binding_id`, `role`, `contact_id: Optional[str]`), zero I/O, same discipline as `src/core/rules.py`/`src/supervisor/logic.py`. Precedence, most to least specific: `(zone_id, reason_code)` → `("*", reason_code)` → `("*", "*")`; fails closed (raises) if even the catch-all is missing, rather than returning an unresolved binding. `DIRECTORY_MAP` ships with exactly one entry — the `("*", "*")` catch-all (`BIND-999`, "General Duty Officer", `contact_id=None`) — deliberately: no placeholder names, phone numbers, or fabricated zone/role data are seeded ahead of real site authorities being identified; real `(zone_id, reason_code)` and `("*", reason_code)` entries get added as those are identified (see Open Items). Applies uniformly to GO and NO_GO alike (GO resolves `reason_code=None`, landing on the catch-all) — the Escalation Requirement already applies to every alert regardless of decision. `OutboundAlert.from_evidence_record()` (`src/maestro/schemas.py`) calls `resolve_authority()` itself and populates `recipient_id` (falls back to the binding's `role` when `contact_id` is unset, rather than fabricating a contact), `authority_binding_id`, `assigned_role`, and `escalation_contact` (built from `directory.SUPERVISOR_OVERRIDE_URL`, a fixed system constant, not a per-binding field — deliberately *not* an escalation URL alone this time, since `assigned_role` now separately carries the human-readable "who," resolving the earlier tension that made a bare URL misleading). `src/airlock/router.py` resolves authority a second time, before `emit_evidence()` runs, so `authority_binding_id` can be part of the *signed* evidence record itself (`None` on GO, same pattern as `reason_code`) rather than appended after signing — a deliberate duplicate call to the same pure, deterministic lookup, not a second decision.
- Schema Provisioning Principle: Schema creation is automatic and idempotent, never a manual step. `src/core/init_db.py` runs once per container start (wired into the Dockerfile's `CMD`, before `uvicorn`) and calls `Base.metadata.create_all()` against `src/core/models.py`'s shared `Base.metadata` — the single source of truth for the schema, so there is no second copy of it (e.g. a raw init-SQL script) to drift out of sync. Every module defining a table (`src/core/models.py`, `src/evidence/models.py`, `src/supervisor/models.py`) must be imported inside `init_db.py`, even if none of its classes are referenced directly — SQLAlchemy only registers a table on `Base.metadata` once its model class has actually been imported somewhere, and this was caught as a real bug during live verification (only `authorized_issuers` was created until the other two model modules were explicitly imported). It retries briefly, since `docker-compose`'s `depends_on` only waits for the `db` container's process to start, not for Postgres itself to be ready. `create_all()` (not Alembic) is the deliberate choice for this project's stage: no production data, no schema-evolution history, one developer — Alembic's versioned-migration machinery would add real overhead (autogenerate discipline, upgrade/downgrade scripts in the deploy path) for no current benefit. Revisit this choice once a schema change needs to preserve existing data, or once `create_all()`'s create-only semantics (no `ALTER`s) become limiting. No seed data ships automatically — `scripts/seed_dev_data.py` is a separate, optional, manually-run script for local dev convenience only.
- ePTW Precondition Principle: High-risk work (`ClaimPayload.work_type` in `EXCAVATION`, `LIFTING`, `HOT_WORK`, `CONFINED_SPACE`) may not proceed without a valid, matching Permit-to-Work. This is Rule 0 in `src/core/rules.py`'s `verify_ptw_precondition`, run before Rule 1 (authority) and Rule 2 (zone safety) in `src/core/evaluator.py`'s `adjudicate()` — a high-risk claim with no valid permit is rejected before Core even considers who submitted it or where. `NOMINAL_CIVIL` work bypasses the gate transparently; it is the only category that does. A permit fails the gate closed if it is missing, not `APPROVED`, outside its `valid_from`/`valid_until` window, mismatched on `zone_id`, or mismatched on permit type (`ClaimPayload.work_type` vs `PtwContext.permit_type`, both the same `WorkType` enum — a same-typed comparison, not a string against unvalidated free text). `PtwContext.valid_from`/`valid_until` must be timezone-aware ISO-8601 strings (`src/airlock/schemas.py` rejects naive or unparseable ones at 422, before Core ever has to compare an ambiguous timestamp against "now"). `Verdict` gained a `reason_code` field, added as a new field rather than by introducing a third `decision` value alongside the existing `"GO"`/`"NO_GO"` vocabulary that `emit_evidence()`, `maestro/schemas.py`'s `OutboundAlert`, and every existing test already depend on.
- Reason Code Convention: `Verdict.reason_code` (`src/core/evaluator.py`) uses a rule-ID-style code, `R-<DOMAIN>-<NUMBER>`, one per rule/failure class — not per individual sub-condition within a rule, so every ePTW sub-check (missing permit, wrong status, outside window, zone mismatch, type mismatch) still reports the same `R-PTW-01`. Defined in `src/core/rules.py` alongside the rule functions they describe: `R-PTW-01` (ePTW Precondition Failure, Rule 0); `R-AUTH-01`/`R-AUTH-02`/`R-AUTH-03` (Authority Failure, Rule 1 — see the 2026-08-06 exception below); `R-ZONE-01` (Safety Violation / Zone Safety Failure, Rule 2); `None` on `GO`. `reason_code` is now threaded all the way into `emit_evidence()`'s persisted evidence record (`src/evidence/emitter.py`) — a rejected claim's signed, persisted evidence carries the specific failure reason, not just a generic `decision="NO_GO"` indistinguishable from any other rejection. It remains additive alongside the existing human-readable `reason` string, which is unchanged. **Deliberate exception, 2026-08-06 (`R-AUTH-01` disambiguation):** Rule 1 now produces three codes, not one — `R-AUTH-01` stays the unauthenticated-issuer case (domain-independent, no honest split possible from claim content); `R-AUTH-02`/`R-AUTH-03` split the insufficient-clearance case by the existing `HIGH_RISK_WORK_TYPES` distinction (`src/core/rules.py`'s `classify_authority_failure()`, called by both `check_authority()` and `adjudicate()` — a deliberate duplicate call to one pure function, same pattern as `resolve_authority()`'s two call sites, not two copies of the branching logic). This does not change `RuleOutcome`/`rule_trace`'s wire shape (`rule_id`/`passed`/`reason` only) — `reason_code` stays a `Verdict`-level field, so `src/maestro/schemas.py`'s `RuleConditionResult` (`extra="forbid"`) is untouched.

### Escalation vs. Override — Decoupling Principle (Locked, 5 Aug 2026)
**Partially superseded the same day by Supervisor Override Retirement, below — this section is kept verbatim for history, not edited, per this file's own convention of recording superseding text as a new entry rather than silently rewriting the old one.** At the time this was written, override still existed as a Supervisor-only capability; the "current implementation note" and "override mechanism" language below describe that now-retired state. The underlying instruction it enforced — Frontline Worker escalation must never present as, or lead to, an override/administrative action — still holds and is carried forward by Supervisor Override Retirement, which applies more broadly now that there is no override to conflate escalation with.

Frontline Worker escalation and Supervisor override are separate concepts and must not be conflated in the Frontline Worker UI.

Current implementation note: `escalation_contact` on `OutboundAlert` resolves to the `/supervisor/override` URL. This is correct and unchanged for Maestro's existing WhatsApp/Telegram adapters. However, if/when a Frontline Worker-facing view is built, it must NOT reuse `escalation_contact` as-is, since doing so would silently present "request help" as "request an override" before override scope itself is resolved (see: Supervisor Override Scope Open Item, pending Willy's sign-off).

v0.1 guidance: Frontline Worker screen shows a contact/escalation path only — no override button, no override language, no override URL exposure. `assigned_role` (the "who") may be shown separately from `escalation_contact` (the override mechanism) once this is actually built — they are already structurally separable in the schema, per verification.

### Supervisor Override Retirement (Locked, 5 Aug 2026)
Supervisor override is retired entirely — not scoped down, removed as a capability. This also resolves the Supervisor Override Scope Open Item (verdict-type-unrestricted override): there is no longer an override to scope.

**Replacement mechanism:** a verdict changes only via a new, verifiable claim submitted by the specific licensed/registered authority role that owns the gate being changed, re-adjudicated fresh through Core (`POST /airlock/claims`). This is not an override and not an appeal — it is Core doing exactly what it already always does (stateless, fresh re-adjudication from live state), triggered by the correct authority submitting new facts, not by an administrative action layered on top of an existing verdict.

**Authority set:** PE, QP, PI, PA, PM, SA. Each must be externally verifiable against PEB or MOM registration — **not implemented in this pass; see Open Items.** `PR` (Permit Receiver) is explicitly excluded from this set: it names the executing worker/crew, the same category as the Frontline Worker persona, not an approving/certifying role.

**What actually changed in the repository (2026-08-05):**
- `POST /supervisor/override` (`src/supervisor/router.py`) now returns `410 Gone` unconditionally, before any database access, `evaluate_override()` call, or Maestro notification. A structurally malformed body still 422s at the Pydantic schema boundary first (unrelated to retirement, unchanged fail-closed behavior).
- `evaluate_override()` (`src/supervisor/logic.py`), `OverrideRecord` (`src/supervisor/schemas.py`), `OverrideAuditEntry`/`override_records` (`src/supervisor/models.py`), and `persist_override_record()` (`src/supervisor/repository.py`) are left intact, per instruction — not deleted, simply no longer called from the retired route. Formal removal is a separate future decision, not made here.
- `src/maestro/directory.py`'s `AuthorityBinding` gained an optional `role_type: Optional[AuthorityRoleType]` field (new `AuthorityRoleType` enum: `PE`/`QP`/`PI`/`PA`/`PM`/`SA`). **Schema/structure only** — no real PEB/MOM registration data is populated, and `DIRECTORY_MAP` still ships with only the original untyped `("*", "*")` catch-all (`BIND-999`, "General Duty Officer", `role_type=None`), deliberately retained: removing it before real typed entries exist would fail closed on *every* Maestro alert and the Frontline Worker screen (`GET /frontline/blocked/{claim_id}`), both of which call `resolve_authority()` — a materially larger and different decision than a schema restructuring, not made here.
- `escalation_contact` (`src/maestro/schemas.py`'s `OutboundAlert.from_evidence_record()`) no longer builds a `directory.SUPERVISOR_OVERRIDE_URL` link — pointing an escalation at a retired endpoint would be actively misleading. It now states the resolved authority directly: `f"{binding.role} ({binding.binding_id})"`, e.g. `"General Duty Officer (BIND-999)"`.
- `render_alert_text()` (`src/maestro/formatting.py`) dropped "escalate/**override**" phrasing in favor of plain "escalate" — applies to every alert, GO or NO_GO, on any channel.
- `frontend/blocked-screen/blocked-screen.js` (the Supervisor Blocked Screen) is **untouched** — it still renders an override submission form pointed at `/supervisor/override`. Submitting it now surfaces the 410 retirement message via the component's existing error-handling path (no code change needed there — `formatErrorDetail()` already surfaces a string `detail` verbatim), but the form itself remains visually present. Removing or repurposing that form is a separate, not-yet-made UI decision — flagged here, not resolved.

**Explicitly not decided in this pass** (see Open Items): binding the PE/QP/PI/PA/PM/SA authority set to real PEB/MOM registration data; the specific `reason_code` → role-type routing table (still a working draft); whether/how `ClaimPayload` should carry which authority role is submitting a re-adjudication claim; formal removal of the now-dead override code paths; any change to `frontend/blocked-screen/blocked-screen.js`'s override form.

### Communications Doctrine (Locked, 1 Aug)
Synapse is strict internally, simple externally.

Scope: all human, machine, sensor, BIM, API, and audit communications.

Constraint: usability optimizations must never compromise deterministic, 
auditable, fail-closed governance behavior.

Open Items (not implemented — do not represent as built): Device PKI, 
telemetry signing, hardware attestation, HSM-backed trust.

**Scope comparison, 1 Aug (Copilot–Gemini Frontline Worker UI review):** re-proposed as "Human interfaces / Machines and robots / Sensors and telemetry sources / BIM and connected systems / APIs and channel adapters / Audit surfaces." Checked against the wording above, category by category: "machine" already covers "machines and robots" (a robot is a machine; no narrower reading was intended or implied); "sensor" already covers "sensors and telemetry sources" (telemetry is sensor-sourced data, not a separate category); "BIM" already covers "BIM and connected systems" under this doctrine's own "all ... communications" framing; "API" already covers "APIs and channel adapters" (Maestro's `WhatsAppAdapter`/`TelegramAdapter` are channel adapters over what this doctrine already treats as one of the enumerated communication surfaces); "audit" and "human" map directly. No genuine scope gap found — **no second entry added, no wording change made.** This doctrine already covers everything the reproposal describes.

### Stage 1 Operational Stories (Locked Classification, 1 Aug — Copilot–Gemini Frontline Worker UI review)
Classifies the frontline-facing interaction stories the Blocked Screen (and any future frontline surface) must represent. Two are current, real behavior, already true of `adjudicate()`/`emit_evidence()` today and test-verified (`tests/test_adjudication.py`, `tests/test_core_eptw.py`); the third is explicitly target-state only.

1. **GO baseline** — valid ePTW, authority verified, zone checks satisfied. Internal `decision`: `GO`. `reason_code`: `None`. Frontline wording: "GO — AUTHORIZED". Render only the actual `rule_trace` data `adjudicate()` returned — no synthesized or assumed passing conditions.
2. **ePTW rejection baseline** — Rule 0 (`verify_ptw_precondition`) fails on an expired permit. Internal `decision`: `NO_GO`. `reason_code`: `R-PTW-01`. Frontline wording: "BLOCKED — PERMIT EXPIRED". Later checks (`authority_check`, `zone_safety_check`) must never be represented as passed — Core short-circuits on Rule 0 failure and never evaluates them at all (`src/core/evaluator.py`'s `adjudicate()`; verified by `tests/test_core_eptw.py::test_adjudicate_rejects_before_authority_check_even_runs`, which asserts `rule_trace` has exactly one entry in this case). Render only evidence actually supplied by Core.
3. **Telemetry or assurance failure** — **TARGET STATE ONLY, NOT IMPLEMENTED.** Requires future constitutional reason codes, evidence schemas, and enforcement behavior that do not exist yet (see Distinct Target-State Assurance Failures, below, and Open Items).

Universal constraint, applying to all three: failure reason codes must never be displayed as, or implied to be, successful rule identifiers. Verified true of the current `blocked-screen.js`/`src/supervisor/router.py` data path as of this writing — `reason_code` and `rule_trace` entries are structurally and visually distinct throughout (`reason_code` renders only in the conflict block via a `.reason-code`-styled span, never inside a `rule-id`-styled, `passed: true` list item) — recorded here as a locked constraint going forward, not a fix to a found violation.

### Stage 2 Frontline Worker Contract (Approved Design Requirements — Implemented 5 Aug 2026 (`src/frontline/router.py`, `frontend/frontline-screen/frontline-screen.js`); originally recorded 1 Aug 2026 as NOT YET IMPLEMENTED — Copilot–Gemini Frontline Worker UI review)
Approved UI/UX requirements for the frontline-facing surface. Implemented as of 5 Aug 2026 (see the header status line above); `frontend/blocked-screen/blocked-screen.js` remains a separate, supervisor-facing component that predates this contract and does not implement it — that distinction is unchanged:

- Mobile-first, single-column layout.
- Plain-language operational verdict, with an unmistakable first instruction: GO → "You may proceed"; NO_GO → "Do not proceed"; unavailable assurance → "Do not operate manually".
- Immediate next step and escalation reference shown alongside the verdict.
- Complete rule trace available through progressive disclosure (not necessarily shown in full up front).
- 48px minimum touch targets.
- Maximum two short sentences in primary operational copy.
- Meaning conveyed through text, icon, and colour together — never colour alone.
- WCAG AA is the mandatory accessibility baseline; WCAG AAA is a design target only, not a requirement.
- Dynamic verdict changes are announced accessibly without unnecessary focus movement.
- `role="alert"` used only where appropriate for dynamic critical state changes — not as a default.
- `BIND-999` displayed as a non-interactive "General Duty Officer" reference; no contact button while `contact_id` remains absent. **Already true of the current live route and component** (`src/supervisor/router.py`'s `GET /supervisor/blocked/{claim_id}`, `blocked-screen.js`'s `_renderAuthorityBinding()`) — confirmed by inspection, not something this contract newly requires building.
- Target-state fixtures (e.g. for the two Distinct Target-State Assurance Failures, below) must be visibly labelled "TARGET STATE / NOT IMPLEMENTED" wherever they appear. `frontend/blocked-screen/demo.html` has no target-state fixtures today — this labelling convention applies to future work, not a retrofit of existing fixtures.

Current gaps against this contract, noted for accuracy, not acted on in this pass (documentation only): `blocked-screen.js` currently uses `role="status" aria-live="polite"` on its root section, not `role="alert"`; it renders a raw `GO`/`NO_GO` decision badge, not the "You may proceed"/"Do not proceed" plain-language copy; touch-target sizing and WCAG conformance have not been audited against this contract.

**Progressive disclosure — Frontline scope correction (Willy, 26 Aug 2026).** The "Complete rule trace available through progressive disclosure" bullet above is corrected, not deleted, per this file's own amend-with-a-dated-marker convention: it applies to the **Supervisor** persona only (`blocked-screen.js`'s existing full rule-trace rendering), not to the Frontline Worker screen. This was found to conflict with `Synapse_UI_Constitutional_Specification_v0.1.md`'s Section 7 ("Explicitly hidden from this screen: rule internals, pipeline/architecture concepts, technical evidence detail, override mechanics"), which the actual build (`src/frontline/router.py`, `frontline-screen.js`) follows — there is no rule-trace access point, progressive or otherwise, anywhere on the Frontline Worker screen, by design. Willy has confirmed the Spec's version is correct and current: this supersedes the progressive-disclosure bullet above for Frontline specifically. The bullet's text is left as originally written for history, per this section's own now-corrected build-status header.

### GO Freshness Principle (Approved in Principle — Copilot/Gemini reviewed, 1 Aug; parameters unresolved and implementation-blocking — see Open Items)
"A GO authorizes execution only while its constitutional validity conditions remain satisfied. Expiry, loss of a mandatory authority dependency, or mismatch in its bound execution context terminates that authority immediately. Continued execution requires a newly adjudicated GO. The UI may display authority, but may neither create, extend, nor revoke it independently."

Boundaries: an expired GO ceases authorizing immediately; revalidation may mint a new GO; Core or an authoritative enforcement boundary (not yet designated — see Open Items) determines freshness; temporary UI connection loss alone does not revoke a GO; a GO remains bound to its original claim, asset, operator authority, zone, permit context, and relevant state version; the UI must not calculate or invent constitutional authority independently. A countdown may supplement, but never replace, "Authorized now" / "Valid until" / "Last verified" display. Visible UX transitions: GO → "You may proceed"; revalidating → "Wait, verification in progress"; expired or invalidated → "Authorization ended, do not proceed".

**Not implemented.** No freshness/expiry concept exists anywhere in the current adjudication or evidence path today (confirmed by inspection: `evaluated_at` is persisted but not currently used for any freshness check anywhere, and the only expiry concept in the codebase — `PtwContext.valid_from`/`valid_until` — is permit validity, a distinct, already-implemented concern, not GO-verdict freshness). The general principle above is approved; the concrete interval, dependency definitions, schema fields, and enforcement mechanics are explicitly unresolved — see Open Items. Do not invent them.

### Two-Track Operational Readiness Model (Locked, 1 Aug)
Synapse's remaining work is formally split into two decoupled tracks, reported separately — never again as a single conflated percentage.

**Track A — Platform Completion:** bounded, lower-risk software/UI work. Frontline GO/BLOCKED experiences, UI consistency, Maestro reason-code presentation, documentation, accessibility, testing, clean demonstration packages.

**Track B — Operational Readiness:** heavyweight real-world deployment work. Site integration, identity/telemetry assurance, real authority directories, system hardening, adversarial testing, deployment procedures, field trials.

Track B is formally ring-fenced: it does not begin until the six open constitutional decisions (GO validity interval, mandatory authority dependencies, signed schema fields, enforcement ownership, future reason codes, assurance-failure evidence schemas — see Open Items' "GO Freshness — unresolved constitutional decisions" entry) are resolved. Resolving those six items is necessary but not sufficient to start Track B — initiation additionally requires Willy's explicit go-ahead. Resolving the six items does not auto-trigger Track B.

**Why this matters, made explicit:** Track B is what the BCA Innovation Sandbox at the Built Environment Innovation Hub targets. The ring-fence exists specifically so Track A UI/demonstration work is never mistaken for Sandbox-ready operational readiness — "Track B remains formally uninitiated pending resolution of the six items" is not just a status label, it is the thing standing between current progress and Sandbox eligibility.

**Excluded scope:** Track B activities — site integration, hardware attestation, live authority linking — must never be smuggled into Track A UI/UX deliverables. Any future handoff touching Track A scope should be checked against this boundary before being accepted as in-scope.

**Reporting language:** no section of `CLAUDE.md` currently states or implies a single completion percentage for Synapse (checked in full, 1 Aug) — there was nothing to retroactively replace. This principle sets the going-forward standard: any future status reporting must report Platform Completion and Operational Readiness as distinct, separately tracked figures, never collapsed back into one number.

**Unresolved, not decided here:** the transition trigger and milestone criteria for actually starting Track B — beyond "the six items are resolved and Willy says go" — remain undefined. No specific milestones or timeline are invented here; see Open Items.

## Open Items
- Docker path — **RESOLVED 2026-07-28.** Docker Desktop is now installed. `docker compose up --build` builds and starts all three containers (`app`, `db`, `cache`) cleanly — verified via container logs (Postgres "database system is ready to accept connections", Redis "Ready to accept connections tcp", Uvicorn "Application startup complete") and via `pg_isready`/`redis-cli ping` in-container. All three ports (5432, 6379, 8000) confirmed reachable from the host. Full pytest suite: 54/54 passing with containers running (note: the HTTP-level tests still use dependency-injected fakes regardless of live infra — that's the locked testability principle, unchanged, not a gap). Beyond the test suite, a genuine live end-to-end request was made directly against the running containers (no mocks, no TestClient): `POST /airlock/claims` returned a real signed `200 GO` for a valid claim and a real signed `200 NO_GO` for an unknown zone — both replacing the earlier documented 500 fail-closed behavior from the non-Docker path.
- Maestro is not wired into `/airlock/claims` — **RESOLVED 2026-07-31, partially.** `src/airlock/router.py` now calls into `src/maestro/` for every NO_GO adjudication (see NO_GO Notification Principle, above) — synchronously in the request path, same as the override endpoint. GO adjudications still trigger nothing, by deliberate decision, not because the wiring is incomplete. Full pytest suite: 78/78 passing, including dedicated coverage that each of the three failure classes fires a Maestro alert with the correct `reason_code`, and that GO fires none.
- Multi-regulator ruleset architecture — **new, 2026-07-28, not decided.** Today's rule set (authority, zone safety, ePTW precondition) is a single, undifferentiated pipeline — there is no concept of separate, addressable regulator jurisdictions (e.g. LTA, BCA, MOM, or any other authority-specific ruleset). Proposed direction, not designed or built: each regulator's rules become their own addressable ruleset, evaluated independently, with an aggregate verdict that fails closed if *any* jurisdiction rejects — consistent with the fail-closed doctrine already governing everything else in Core. Needs a real design pass (how rulesets are registered/addressed, how the aggregate verdict's rule_trace represents multiple jurisdictions at once) before implementation.
- Cross-regulator contradiction handling — **new, 2026-07-28, explicitly deferred.** A genuine conflict case — one jurisdiction's ruleset approving what another's rejects — has not been specified, and this item is deliberately not designed against a hypothetical. Planned fallback, once multi-regulator rulesets (above) exist and a real conflict case is specified: a contradiction produces `PENDING_HUMAN_REVIEW` rather than either an automatic approval or an automatic rejection. Not implemented; not even schema-designed yet.
- `reason_code` not threaded into persisted evidence — **RESOLVED 2026-07-31.** `emit_evidence()` now includes `reason_code` in the signed, persisted `AdjudicationRecord` (see Reason Code Convention, above). All three failure classes (`R-PTW-01`, `R-AUTH-01`, `R-ZONE-01`) are covered; `None` on `GO`. Full pytest suite: 71/71 passing, including dedicated coverage that the signature actually covers `reason_code` (tampering with it invalidates the signature, same as any other field).
- Downstream consumption of `reason_code` unconfirmed — **still open, 2026-07-31.** `src/maestro/schemas.py` and `src/maestro/formatting.py` still branch only on `decision`/`conflicting_condition` and were deliberately left untouched this pass (see Changelog for the recommendation) — this was a documentation gap plus an evidence-emitter change, not a decision about Maestro's contract. `decision="NO_GO"` is still shared by both Authority Failure and PTW Failure at the Maestro/`OutboundAlert` layer; only the persisted evidence record now distinguishes them.
- No frontend stack decided — **still not chosen, 2026-07-31 update: the component is now live.** `frontend/blocked-screen/` (the supervisor Blocked Screen component) was built framework-agnostic — a zero-dependency vanilla-JS Web Component, no `package.json`, no build step — specifically so building it wouldn't itself decide React vs. Vue vs. server-rendered templates vs. anything else. As of 2026-07-31 it's reachable in the running app at `GET /supervisor/blocked/{claim_id}` (`src/supervisor/router.py`, served via `src/main.py`'s `/static` mount) with real persisted data — going live didn't force the framework decision either; the same plain JS still just gets served as a static asset. That decision is still open; whoever makes it will either keep this component as-is or port it into whatever's chosen. See `frontend/README.md`.
- `telemetry_signed` is a display-only placeholder — **new, 2026-07-31, not implemented.** `frontend/blocked-screen/`'s telemetry-trust badge reads a single caller-supplied `telemetrySigned: boolean | undefined` field with no backing data anywhere in the backend (`src/core/rules.py`'s `ZoneRecord` has no such attribute) and no signing/attestation/HSM/device-PKI mechanism at all — explicitly out of scope per this session's instructions. Whoever wires real telemetry trust into Core/Redis later should treat this as the UI's existing hook, not a green light to skip real verification.
- `escalation_contact` misleading placeholder text — **RESOLVED 2026-07-31.** Replaced with a real, deterministic mechanism: `src/maestro/directory.py`'s `resolve_authority(zone_id, reason_code)` (see Escalation Ownership Principle, above). `recipient_id`/`escalation_contact`/`authority_binding_id`/`assigned_role` are now resolved facts, not caller-supplied or hardcoded strings.
- Authority directory sparsely populated — **new, 2026-07-31, expected, not a bug.** `DIRECTORY_MAP` (`src/maestro/directory.py`) has exactly one entry, the `("*", "*")` catch-all — every claim today, regardless of zone or failure reason, resolves to `BIND-999`/"General Duty Officer"/`contact_id=None`. This is intentional for this pass (no fabricated authorities), but it means `recipient_id` currently falls back to a role name rather than a real deliverable contact (WhatsApp number, Telegram handle, etc.) for every single alert. Real `(zone_id, reason_code)` and `("*", reason_code)` entries — and real `contact_id` values — need to be added as actual site authorities are identified; not designed here, no timeline. **UPDATE 2026-08-18:** no longer sparsely populated in the sense originally described. `DIRECTORY_MAP` now routes six of Core's reason_code values — `R-ZONE-01` → `SA` (unchanged, 2026-08-06); `reason_code=None` (GO), `R-PTW-01`, `R-AUTH-01`, `R-AUTH-02`, `R-AUTH-03` → `RTO` (direct confirmation, commit `19ad005`) — plus QP and QE together via the orthogonal `is_design_alteration` flag (`resolve_authority()` now returns `list[AuthorityBinding]`, commit `a3e2b03`). Only the untyped `("*", "*")` catch-all remains for any reason_code not explicitly named — no longer every claim's default. Still genuinely true and unresolved, unchanged by this update: every `contact_id` on every binding is still `None` — `recipient_id` still falls back to a role name, not a real deliverable contact, for every single alert. Real `contact_id` values still need to be added as actual site authorities are identified; not designed here, no timeline.
- GO Freshness — unresolved constitutional decisions, **implementation-blocking, 1 Aug.** The GO Freshness Principle (above) is approved in principle; none of the following are decided, and none should be invented: (1) the actual GO validity interval and governing policy; (2) the definition of "mandatory dependencies" whose loss terminates authority; (3) the exact signed schema fields for `evaluated_at`, `valid_until`, policy version, and contextual bindings (none of these beyond `evaluated_at` exist in `Verdict`/the persisted evidence record today); (4) enforcement ownership and transition mechanics — which component is the "authoritative enforcement boundary" the principle refers to, and how a GO transitions to expired/revalidating; (5) future reason codes for freshness-related rejections; (6) evidence schemas for assurance failures (see next item). Any implementation of GO freshness is blocked on these being decided elsewhere, not in code.
- Distinct target-state assurance failures — **not implemented, 1 Aug, kept deliberately separate.** Two different failure modes, both future fail-closed `NO_GO` outcomes, neither implemented, neither sharing a reason code, evidence schema, or UX copy with the other: **Telemetry unavailable** (no trustworthy data received at all; frontline UX "STATUS UNAVAILABLE — DO NOT PROCEED"; frontline action: check connectivity and contact system support) vs. **Cryptographic assurance unknown** (data was received, but source identity/signature/attestation cannot be authenticated; frontline UX "UNVERIFIED SOURCE — DO NOT PROCEED"; frontline action: stop and contact system support). HSM/attestation-agent diagnostics belong to a supervisor or maintenance disclosure surface, not the frontline primary view, once built. Neither capability exists anywhere in this codebase today — no reason codes, no schema fields, no UI states. See Stage 1 Operational Stories' "Telemetry or assurance failure" story (above), which is the same target-state item.
- Rule 0B — Condition Precedent (SOPA Payment Claim Timing) — **Still Open, updated 2026-08-06.** Status unchanged: on hold, pending real QS/legal input. A design proposal was drafted (see session dated 6 Aug 2026) based on a read of SOPA ss.10(2)/10(3)/11(1)/12/13, SOPR reg 5(1)/5(3), and *Asia Grand Pte Ltd v A I Associates Pte Ltd* [2023] SGHC 175 — but this was Willy and Claude reading primary sources directly, not QS-grounded confirmation. This does not satisfy the original condition ("no implementation until Willy confirms scope resolved with Ben Chin, or equivalent qualified QS/legal input"). Ben Chin is not currently involved. Remains blocked on finding equivalent expertise, not on the reasoning quality of the draft itself.

  Two new sub-questions surfaced by the draft, to resolve whenever real input is available:
  1. **Schema fit.** `ClaimPayload`'s existing shape (`zone_id`, `work_type`, `ptw_context`) is built for site-safety/permit claims. A SOPA payment claim isn't naturally a site-safety claim — it has no zone, no work_type in that sense. Open question: does Rule 0B fit as an optional bolt-on context (`PaymentClaimTimingContext`), or does it need a genuinely separate claim shape/endpoint? This may be the first concrete instance of the already-flagged, undesigned multi-regulator ruleset architecture problem — not a variant of something already solved.
  2. **Evidence emission.** The proposed gate sits at Airlock, before `adjudicate()` — meaning a failing claim today would return a plain 422 with no signed record, no audit trail. A SOPA timing rejection is a legally consequential determination, not a malformed-JSON error. Open question: does Rule 0B need its own evidence-emission path, distinct from both a plain Airlock 422 and a Core NO_GO, to stay consistent with the system's fail-closed, everything-signed doctrine? Not resolved, not assumed either way.

  The draft computation logic itself (deemed service date → response due → dispute settlement end → 7-day adjudication window, both edges enforced) is preserved in session notes for whenever real QS input unblocks this — not lost, just not actioned.
- Supervisor Override Scope unrestricted by verdict type — **SUPERSEDED 2026-08-05 by Supervisor Override Retirement, above.** Original finding, kept for history: `evaluate_override()` (`src/supervisor/logic.py:33-62`) did not branch on `reason_code` or `decision`; `POST /supervisor/override` applied no verdict-type filtering, so it could override a Safety Violation (`R-ZONE-01`) verdict, not just a commercial one. The proposed resolution below was never locked or implemented — override was retired entirely instead, which resolves this item by removing the capability the scoping question was about, not by answering the question.
- PE/QP/PI/PA/PM/SA authority set not bound to real registration data — **new 2026-08-05, updated 2026-08-06 (twice).** `AuthorityRoleType` (canonically `src/core/roles.py`) now has one live `DIRECTORY_MAP` entry (`R-ZONE-01` → `SA`, Task 3) and two confirmed human-readable labels (`ROLE_TYPE_LABELS`: PE, QP — Task A), but still zero connection to real PEB/MOM registration records anywhere in this codebase, and `IssuerRole` (`src/core/models.py`) still has no real rows — `fetch_issuer_roles()` returns an empty list for every issuer. Not designed here, no timeline. A future `verified`/`registration_id` column on `IssuerRole` would live naturally per role-row — the schema doesn't preclude this, but nothing beyond the row shape itself is built. Every reason_code except `R-ZONE-01` still resolves to the untyped `BIND-999`/"General Duty Officer" catch-all (see the routing Open Item, above). **UPDATE 2026-08-18:** PI removed entirely as a confirmed category error (2026-08-14) — the set is now PE/QP/PA/PM/SA/QE/RTO, seven codes, not six-plus-PI. `DIRECTORY_MAP` now has six live routing entries, not one: `R-ZONE-01` → `SA` (unchanged), `reason_code=None`/`R-PTW-01`/`R-AUTH-01`/`R-AUTH-02`/`R-AUTH-03` → `RTO` (direct confirmation, commit `19ad005`), plus QP and QE together on the `is_design_alteration` flag (commit `a3e2b03`) — so "every reason_code except R-ZONE-01 still resolves to the... catch-all" is no longer accurate; only a reason_code outside Core's current rule set would. Still genuinely unresolved, unchanged by this update: zero connection to real PEB/MOM/IES/ACES registration records anywhere in this codebase; `IssuerRole` still has no real rows. QP/QE firing together (rather than being distinguished per-claim) on every design-alteration claim is a related but distinct open question — flagged separately in this file's Changelog (2026-08-18) as an unconfirmed placeholder default, not a domain decision.
- `reason_code` → authority role_type routing table — **new 2026-08-05, partially resolved 2026-08-06.** Only `R-ZONE-01` → `SA` is encoded (`src/maestro/directory.py`'s `DIRECTORY_MAP`, `("*", "R-ZONE-01")`), the one mapping confirmed unambiguous. `R-PTW-01` remains unrouted: the handoff that introduced this routing paired it with "PI/PA" together, not a single role, and `AuthorityBinding.role_type` holds one value — nothing confirms which of the two (or whether either) is the actual answer, so nothing was guessed. `R-AUTH-01`/`R-AUTH-02`/`R-AUTH-03` remain unrouted too: `R-AUTH-01` (unauthenticated issuer) isn't a domain/technical failure — no PE/QP/PI/PA/PM/SA role honestly fits "we don't know who submitted this"; `R-AUTH-02`/`R-AUTH-03` are new (2026-08-06, R-AUTH-01 disambiguation) and postdate the original "clean" assessment entirely, so nothing has actually confirmed a role for them. Do not invent these; each can gain its own entry the same way `R-ZONE-01` just did, once a real answer exists. **RESOLVED (further) 2026-08-18.** `R-PTW-01` and `R-AUTH-01`/`R-AUTH-02`/`R-AUTH-03` are no longer unrouted — direct confirmation (2026-08-18, explicit, not speculative; see this file's Changelog) routes all four, plus `reason_code=None`/GO, to `RTO`, not `PA` as the original R-PTW-01/PI/PA pairing speculated. This supersedes the reasoning above (kept for history, not deleted): PI has separately been removed as a category error (2026-08-14), and PA's per-project liability assignment remains independently unconfirmed — R-PTW-01 routing to RTO does not confirm or resolve that PA question, it replaces the need to answer it for routing purposes. R-ZONE-01 → SA is unaffected. `AuthorityRoleType` also gained QE and RTO (2026-08-14) and now routes QP and QE together on the orthogonal `is_design_alteration` flag (2026-08-18, commit `a3e2b03`) — see the authority-set Open Item above for that item's own caveat about this being an unconfirmed placeholder default.
- Whether/how `ClaimPayload` should identify the submitting authority's role — **new 2026-08-05, options presented 2026-08-06, still not decided.** The Supervisor Override Retirement's replacement mechanism (a fresh claim from the specific authority role that owns the gate) doesn't yet have a schema field capturing which role submitted a given claim — sharpened by the 2026-08-06 `IssuerRole` migration, since an issuer can now hold *several* roles, so "the" role isn't even well-defined without this. Three options were presented with trade-offs, none chosen: (1) a new optional `ClaimPayload` field (self-declared, non-breaking, unverified by itself); (2) extending `AuthorizedIssuer.role` (reuses an existing but currently-unused-by-logic field, one role per issuer, doesn't fit the corrected multi-role model as cleanly); (3) a separate `issuer_id`-keyed binding table (fully decoupled, but a third source of truth alongside `AuthorizedIssuer.role` and `IssuerRole`). Not designed here.
- Formal removal of retired override code — **new, 2026-08-05, not decided.** `evaluate_override()`, `OverrideRecord`, `OverrideAuditEntry`/`override_records`, and `persist_override_record()` are intentionally still in the repository (per instruction, pending confirmation nothing else depends on them) even though `POST /supervisor/override` no longer calls them. No timeline for actually deleting them.
- `frontend/blocked-screen/blocked-screen.js`'s override form now points at a retired endpoint — **new, 2026-08-05, not decided.** The Supervisor Blocked Screen still renders an override submission form; submitting it now surfaces the `410 Gone` retirement message via the component's existing error-handling path, but the form UI itself hasn't been removed or repurposed. Not designed here.
- Certified Profile module — schema/structure only, not wired into adjudication, **new, 2026-08-11.** `src/profiles/` (`schemas.py`'s `CertifiedProfile`/`BaseProfileRef`/`ProfileLineage`, `models.py`'s zero-row `certified_profiles` table, `repository.py`'s `fetch_certified_profile()`) plus `src/core/profile_resolution.py`'s pure `resolve_effective_parameters()` (STANDALONE passthrough; BASE_ANNEX merge, annex values overriding base, with `BaseProfileMissingError`/`BaseProfileMismatchError` fail-closed on inconsistency) landed this pass, commit `52688ea`. Deliberately kept separate from the Multi-regulator ruleset architecture item above — that item is multiple regulators (LTA/BCA/MOM) each running an independent ruleset within one jurisdiction, with an aggregate verdict that fails closed if any regulator rejects; this module is one jurisdiction's own base+annex code-parameter layering (EN Eurocode + National Annex precedent) or a standalone code (Singapore precedent) — a different axis of the problem, not a substitute for that still-undesigned item, not merged with it. Not wired into `src/core/rules.py`/`evaluator.py`/`src/airlock/` anywhere — no rule consults a `CertifiedProfile` today, no endpoint accepts, persists, or resolves one outside the test suite. Zero rows shipped, same no-fabricated-data discipline as `src/telemetry/models.py`'s `DeviceRegistryEntry` and `src/intake/models.py`'s `IdentityCrosswalkEntry` — no real Eurocode part, national annex value, or Singapore profile parameter exists in `certified_profiles`. `base_profile_version` is pinned (not a floating "latest"), keeping `resolve_effective_parameters` deterministic per this file's Core doctrine. Full suite: 151/151 passing (139 existing + 12 new). Not decided/not designed here: how or where a claim/endpoint would reference a `CertifiedProfile`, whether profile resolution ever feeds into `adjudicate()`'s rule set, and how this interacts with the still-undecided multi-regulator architecture once that item itself gets designed.

## Technology Stack & Infrastructure
- Language & Runtime: Python 3.11+
- API Framework: FastAPI (Async, strict Pydantic v2 validation models)
- Database Layer: PostgreSQL (Rule registry & immutable audit storage) via SQLAlchemy Async
- Cache & State Store: Redis (Real-time zone state & telemetry cache)
- Containerization: Docker & Docker Compose
- Testing & Coverage: Pytest & HTTPX (100% path coverage required on adjudication logic)

## Repository Layout
```
synapse/
├── CLAUDE.md                   # System Constitution
├── docker-compose.yml          # Infrastructure orchestration (PostgreSQL, Redis, App)
├── Dockerfile
├── requirements.txt
├── src/
│   ├── main.py                 # FastAPI Entrypoint
│   ├── airlock/                # Schema validation & fail-closed ingestion
│   ├── core/                   # Deterministic Adjudication Engine (Pure functions)
│   │   ├── rules.py            # Pure rule checks (authority, zone safety) — unchanged by the IssuerRole migration
│   │   ├── evaluator.py        # Pure adjudication orchestration — unchanged by the IssuerRole migration
│   │   ├── roles.py             # AuthorityRoleType (canonical home, 2026-08-06) — re-exported from src/maestro/directory.py
│   │   ├── models.py           # SQLAlchemy ORM models (rule registry) — schema source of truth; IssuerRole (2026-08-06) added, not yet read by rules.py/evaluator.py
│   │   ├── repository.py       # PostgreSQL/Redis lookups (I/O boundary); fetch_issuer_roles() (2026-08-06) is read-access only, not wired into adjudication
│   │   ├── profile_resolution.py # resolve_effective_parameters() (2026-08-11) — pure STANDALONE/BASE_ANNEX merge, not wired into rules.py/evaluator.py
│   │   └── init_db.py          # Schema provisioning (create_all), run via Dockerfile CMD before uvicorn
│   ├── evidence/                # SHA-256 JSON-LD immutable log emitter
│   │   ├── emitter.py           # emit_evidence, emit_override_evidence (pure, no I/O)
│   │   ├── models.py            # AdjudicationAuditEntry (persisted, append-only)
│   │   └── repository.py        # persist/fetch adjudication records (I/O boundary)
│   ├── maestro/                 # Channel-agnostic delivery layer (wired for NO_GO adjudications; override notifications retired — see Open Items)
│   │   ├── schemas.py            # OutboundAlert (resolves escalation authority via directory.py)
│   │   └── directory.py          # AuthorityBinding (+ optional role_type: AuthorityRoleType) + resolve_authority() — deterministic (zone_id, reason_code) lookup, zero I/O
│   ├── supervisor/              # Admin-override: HTTP endpoint (RETIRED, 410 Gone), pure logic, persistence — all left intact but non-functional, see Supervisor Override Retirement
│   │   ├── schemas.py           # OverrideRecord (fail-closed input schema; still used only to keep malformed-body 422 behavior)
│   │   ├── logic.py             # evaluate_override (pure, no I/O) — no longer called live
│   │   ├── models.py            # OverrideAuditEntry (persisted, append-only) — no longer written to
│   │   ├── repository.py        # persist override records (I/O boundary) — no longer called
│   │   └── router.py            # POST /supervisor/override → 410 Gone unconditionally; GET /supervisor/blocked/{claim_id} unaffected
│   ├── frontline/               # Frontline Worker persona — first implementation 2026-08-05, separate from supervisor/
│   │   └── router.py             # GET /frontline/blocked/{claim_id} — renders GO and NO_GO alike (no 409); resolves a real assigned_role via resolve_authority()
│   ├── profiles/                # Certified Profile registry — schema/structure only, not wired into adjudication (2026-08-11)
│   │   ├── schemas.py            # CertifiedProfile, BaseProfileRef, ProfileLineage (STANDALONE / BASE_ANNEX)
│   │   ├── models.py             # CertifiedProfileRecord (persisted, zero rows shipped)
│   │   └── repository.py         # fetch_certified_profile() (I/O boundary)
│   └── config.py               # Environment & state configurations
├── scripts/
│   └── seed_dev_data.py        # Optional, manual-only dev seed data — never run automatically
├── frontend/                   # No stack decided yet — see Open Items. Vanilla-JS Web Components only, deliberately dependency-free. Served at /static (src/main.py).
│   ├── README.md                # Schema grounding, telemetry_signed placeholder caveat, open decisions, live routes
│   ├── blocked-screen/          # Supervisor-facing verdict display (Locked Supervisor UI Principle, Escalation Requirement) — live at GET /supervisor/blocked/{claim_id}
│   │   ├── blocked-screen.js     # <blocked-screen> custom element (styles inlined); override form now posts to a retired (410) endpoint — see Open Items
│   │   └── demo.html             # Standalone fixture-driven demo, no build step
│   └── frontline-screen/        # Frontline Worker-facing verdict display (Stage 2 Frontline Worker Contract) — live at GET /frontline/blocked/{claim_id}
│       ├── frontline-screen.js   # <frontline-screen> custom element — no rule internals, no override form/URL/language, by construction
│       └── demo.html             # Standalone fixture-driven demo, no build step
└── tests/
    ├── test_airlock.py         # Fail-closed schema boundary tests
    ├── test_airlock_maestro.py # POST /airlock/claims -> Maestro wiring: NO_GO alerts (all failure classes), GO fires none
    ├── test_adjudication.py    # Deterministic rule evaluation scenarios
    ├── test_core_eptw.py       # ePTW precondition gate: fail-closed branches + adjudicate() ordering
    ├── test_core_repository.py # fetch_issuer_roles() (2026-08-06); IssuerRole multi-role shape; AuthorityRoleType canonical-import identity check
    ├── test_evidence.py        # Cryptographic hashing & audit output tests (adjudication + override records, authority_binding_id)
    ├── test_maestro_*.py       # Maestro schema, formatting, and adapter tests
    ├── test_maestro_directory.py # resolve_authority() precedence: specific zone+reason > global reason default > catch-all; AuthorityRoleType schema tests
    ├── test_supervisor_schemas.py  # OverrideRecord schema validation (schema itself still valid, just unused live)
    ├── test_supervisor_logic.py    # evaluate_override pure-logic tests (function itself still valid, just unused live)
    ├── test_supervisor_router.py   # POST /supervisor/override: 422 malformed body, 410 Gone for every other case, zero DB access
    ├── test_supervisor_blocked_screen.py  # GET /supervisor/blocked/{claim_id}: 200 real NO_GO data, 404 unknown, 409 GO, static JS served
    ├── test_frontline_router.py    # GET /frontline/blocked/{claim_id}: 200 for GO and NO_GO alike, 404 unknown, static JS served, no rule/override leakage
    ├── test_profiles_schemas.py    # CertifiedProfile lineage/base_ref validator: both valid shapes, both invalid combinations
    ├── test_profiles_repository.py # fetch_certified_profile(): none-on-miss, STANDALONE row, BASE_ANNEX row with base_ref
    └── test_core_profile_resolution.py # resolve_effective_parameters(): STANDALONE passthrough, BASE_ANNEX merge, missing/mismatched base errors
```

## Developer Directives
- Implement, do not redesign. Architectural boundaries above are locked.
- Any payload missing required schema fields or failing validation MUST return HTTP 422 / Schema Error immediately — no silent defaults, no best-effort parsing.
- Keep `src/core/` pure, deterministic, and testable without database side-effects during evaluation.

## Multi-Agent Operating Model
Governs how Copilot, Gemini, Claude (chat), Claude Code, and human authority collaborate on *this repository's development process*. This section has no runtime effect and grants no runtime authority to any agent — Core adjudicates runtime authority (GO/NO_GO), not Copilot, Gemini, Claude, or any other agent. Prompts govern collaboration only.

### Role Separation (Recorded 2026-08-01)
- **Copilot** drafts contracts and reconciles architecture.
- **Gemini** independently reviews UX, accessibility, field ergonomics, and edge cases.
- **Claude (chat)** performs conversational synthesis and continuity across sessions.
- **Claude Code** performs repository inspection, scoped implementation, testing, and reporting.
- **Human authority** (Willy) resolves constitutional questions before any dependent implementation proceeds.

### Structured Handoff Schema (Recorded 2026-08-01)
Standard format for inter-agent handoffs going forward: `authority` / `task` / `facts` / `approved_contract` / `unresolved` / `excluded_scope` / `acceptance_tests` / `report`.

### Execution Evidence Authority (Locked, 1 Aug)
Execution Evidence Authority (Locked, 1 Aug): Only tools with actual repository/filesystem access can produce execution evidence — git status, file contents, test results, diffs. This currently means Claude Code sessions run directly against this repository, or Willy's own local commands.

Gemini and Copilot have no access to this repository or to CLAUDE.md unless Willy explicitly pastes content into their conversation. Any output from them — including content formatted as an "audit report," "execution notice," or "compliance matrix" with file/line citations, git hashes, or diff summaries — is advisory input for reconciliation, never execution evidence. It must not be treated as ground truth about repository state, and must not be acted on until independently verified against the actual repository by a tool with real access.

Practical checks: a real git commit hash is exactly 40 hexadecimal characters. A cited process name, stage, or contract is only real if it can be found in CLAUDE.md or git history.

When a report claiming repository execution cannot be verified as coming from a tool with actual access, verify its specific claims against live repository state before accepting or acting on any of them.

### Oversight and Support Roles (Locked, 1 Aug)
Oversight and Support Roles (Locked, 1 Aug): Claude and Claude Code hold oversight on Synapse. Gemini and Copilot operate in a support role.

This supersedes the earlier 1 Aug framing that Copilot and Gemini would "lead" the Synapse build. That framing is retired.

Rationale: Gemini and Copilot have no repository or CLAUDE.md access unless Willy explicitly pastes content to them (see Execution Evidence Authority, above). Oversight requires the ability to verify claims against actual repository state — something only Claude Code (running directly against this repo) and Willy himself can do. Gemini and Copilot remain valuable for independent UX critique, contract drafting, and analysis, but decisions about what is true of the repository, and final sign-off on what gets implemented or recorded as locked, rest with Claude/Claude Code and Willy.

This does not change the existing Synapse UI Workflow Gate Order (Copilot defines → Gemini critiques → Copilot reconciles → Claude Code implements) — that sequence still stands. It changes who has final authority when a conflict, contradiction, or unverifiable claim arises: Claude/Claude Code's verification against the actual repo governs, not Gemini or Copilot's unverified output.

### Synapse UI Workflow Gate Order (Locked, 1 Aug)
(1) Copilot defines and normalizes the contract; (2) Gemini critiques usability, clarity, accessibility, field conditions, and edge cases; (3) Copilot reconciles findings against Synapse doctrine; (4) Claude Code inspects the repository, implements, tests, and reports.

A separate 7-stage proposal (Copilot drafts → Gemini reviews → Copilot/Claude reconcile → human resolves constitutional questions → Claude Code implements → Gemini validates → human final acceptance) was submitted in a prior session but remains undecided — not recorded here, not superseding this locked 4-stage sequence.

## Changelog
- 2026-07-30 — Merged `feature/eptw-precondition-gate` into `master` via fast-forward (no other branches had diverged from `master` since the feature branch was created). Merge commit: `7b00607083f14c616c40400cf22c9f18786e2d6e`. Full pytest suite: 68/68 passing, re-run against merged `master` (not just the feature branch in isolation). Remote branch cleanup: local and remote `feature/eptw-precondition-gate` left in place per no explicit deletion request; `master` pushed to `origin/master` at the same commit.
- 2026-07-31 — Threaded `reason_code` into `emit_evidence()`'s persisted evidence record (see Reason Code Convention, above). Adopted `R-<DOMAIN>-<NUMBER>` reason codes for all three existing failure classes (`R-PTW-01`, `R-AUTH-01`, `R-ZONE-01`), replacing the ad hoc `"FAIL_CLOSED_EPTW_PRECONDITION"` value the ePTW gate previously used for `Verdict.reason_code` (the human-readable `reason` string, which still contains that text as a prefix, is unchanged). Full pytest suite: 71/71 passing. **Recommendation, not a decision:** `src/maestro/schemas.py`/`formatting.py` should very likely branch on `reason_code` too — right now `decision="NO_GO"` is the only thing `OutboundAlert`/`render_alert_text` see, and `conflicting_condition` (the first failed `RuleConditionResult`) already gives a supervisor the specific rule, so the practical gap is narrower than it first looks; but a Maestro-facing `OutboundAlert.reason_code` field would make automated routing/filtering on failure class possible without parsing `reason` text. Left undecided and unimplemented this pass, per instruction not to decide unilaterally — see Open Items.
- 2026-07-31 — Added `frontend/blocked-screen/`, a supervisor-facing Blocked Screen enforcing the Supervisor UI Principle and Escalation Requirement by construction: renders every rule_trace entry with its Rule ID, calls out the specific conflicting condition on NO_GO, always renders the escalation contact, and submits overrides to the real `POST /supervisor/override` (verified live: a genuine `fetch()` POST with the correct `OverrideRecord` shape, observed on the network layer, not a mock/timeout — the failure path was also verified live, since no backend was running for this check). Grounded directly in the real `Verdict`/evidence, `OverrideRecord`, and `OutboundAlert` shapes — no invented field names. Two things deliberately **not** decided here, both flagged in Open Items rather than picked unilaterally: (1) no frontend framework was chosen — the component is a dependency-free vanilla-JS Web Component specifically so it wouldn't force that choice; (2) `telemetry_signed` is a single caller-supplied boolean placeholder with no backing implementation — no signing, attestation, HSM, or device PKI, out of scope per instruction.
- 2026-07-31 — Two related changes, committed separately: (1) added `reason_code` to `OutboundAlert` (`src/maestro/schemas.py`), sourced from the same `Verdict.reason_code`/evidence-record `reason_code` already established — field made available only, no adapter/formatting logic consumes it yet, by explicit scope. (2) Wired `src/airlock/router.py` to call Maestro on every NO_GO adjudication (any failure class), never on GO — see NO_GO Notification Principle, above — resolving the "Maestro is not wired into `/airlock/claims`" Open Item (partially: GO still doesn't notify, by design). Full pytest suite: 73/73 after (1), 78/78 after (2).
- 2026-07-31 — Replaced ad hoc `escalation_contact` handling with a real, deterministic mechanism: `src/maestro/directory.py`'s new `AuthorityBinding`/`resolve_authority(zone_id, reason_code)` (see Escalation Ownership Principle, above). `OutboundAlert` gained `authority_binding_id`/`assigned_role`; `OutboundAlert.from_evidence_record()`'s signature changed (`recipient_id`/`escalation_contact` params removed — it now resolves and populates all four escalation fields itself, taking `zone_id` instead, since that's needed for the lookup and isn't on the evidence record). `emit_evidence()` gained an `authority_binding_id` parameter so the resolved binding is part of the *signed* evidence record for NO_GO (`None` on GO) — `src/airlock/router.py` resolves authority before calling it, not after. `DIRECTORY_MAP` ships with exactly one entry, the `("*", "*")` catch-all (`BIND-999`) — deliberately no fabricated authorities. Full pytest suite: 88/88 passing, including dedicated `resolve_authority()` precedence tests (specific zone+reason beats global reason-code default beats catch-all, verified against a monkeypatched multi-entry map since the real `DIRECTORY_MAP` only has one entry) and evidence-signature coverage for `authority_binding_id` (tampering with it invalidates the signature, same as `reason_code`). **Note on the task prompt:** it included a "current master behavior" code snippet and two large example-implementation blocks that contradicted its own explicit numbered instructions (a required `contact_id: str` and an `escalation_url` field on `AuthorityBinding`, multiple fabricated phone/WhatsApp/Telegram directory entries, and an `OutboundAlert` with different field names entirely) — verified against the actual repo state and did not follow those blocks; implemented the explicit "Do NOT" instructions instead. `frontend/blocked-screen/` untouched.
- 2026-07-31 — The Blocked Screen is now live, not just a standalone component: `GET /supervisor/blocked/{claim_id}` (`src/supervisor/router.py`) fetches the real persisted evidence record via `fetch_latest_adjudication_record()` (same helper `POST /supervisor/override` already used — same query pattern followed, not reinvented) and serves `<blocked-screen>` populated with it via a JSON hand-off, with a small string-level escape to stop any user-submitted string (e.g. a `zone_id`/`ptw_id` embedded in `reason` text) from breaking out of the `<script>` tag. 404 if the claim has no adjudication record at all; 409 if it does but the decision is GO (a different failure mode than not existing — this route is NO_GO-only, per the task's explicit instruction not to render Blocked for a GO or unknown claim). `src/main.py` mounts `frontend/` at `/static` so the browser can load `blocked-screen.js` — plain static file serving, not a build step, no new frontend-stack decision made. Verified live in an actual browser (not just `TestClient` string assertions): navigated to the route against a running server, inspected the rendered shadow DOM directly — real claim data, real rule trace, no console errors. Component updated to match the current real schema: `data.evidence.authority_binding_id` (real, persisted) and top-level `data.assignedRole` (mirrors `OutboundAlert.assigned_role`, **not** persisted in evidence) are now documented and rendered — the live route deliberately does not call `resolve_authority()` (out of scope per instruction), so it populates `authority_binding_id` but leaves `assignedRole` unset; `demo.html`'s fixtures show both populated. No new CSS, no new frontend framework, no changes to `src/maestro/directory.py`. Full pytest suite: 88/88 → 92/92 passing (route returns real NO_GO data, 404, 409, and static JS served).

### 1 Aug 2026
LOCKED: Communications Doctrine added to Locked Design Principles — 
"Synapse is strict internally, simple externally," scope covers all 
human/machine/sensor/BIM/API/audit communications, usability never 
compromises deterministic/auditable/fail-closed governance. Open assurance 
requirements (Device PKI, telemetry signing, hardware attestation, 
HSM-backed trust) logged as not-yet-implemented, not folded into the 
locked principle as built.
- 2026-08-01 — Documentation-only update recording the completed Stage 1/Stage 2 Frontline Worker UI design work (Copilot–Gemini review). No production code or tests changed; 92/92 tests unaffected. **Communications Doctrine:** checked the reproposed scope wording ("Human interfaces / Machines and robots / Sensors and telemetry sources / BIM and connected systems / APIs and channel adapters / Audit surfaces") against the existing 1 Aug entry — every category is already covered by the existing wording (see the scope-comparison note appended to that entry); no duplicate principle added, no wording change made, since none was genuinely needed. **New Locked Design Principles added:** (1) Stage 1 Operational Stories — GO baseline (`reason_code: None`, "GO — AUTHORIZED") and ePTW rejection baseline (`reason_code: R-PTW-01`, "BLOCKED — PERMIT EXPIRED", later checks never shown as passed when Core short-circuits) are current, real, test-verified behavior; the third story (telemetry/assurance failure) is explicitly target-state only. (2) Stage 2 Frontline Worker Contract — approved UI/UX requirements for a frontline-facing surface that does not exist yet; `blocked-screen.js` is a supervisor-facing component that predates this contract and does not implement it (specific gaps noted inline: `role="status"` not `role="alert"`, no plain-language "You may proceed"/"Do not proceed" copy, touch targets and WCAG conformance unaudited) — one requirement (`BIND-999` non-interactive, no contact button) is already true today, noted as such rather than claimed as newly built. (3) GO Freshness Principle — approved in principle (Copilot/Gemini reviewed); not implemented; no freshness/expiry concept exists anywhere in the current adjudication or evidence path (the only expiry concept in the codebase, `PtwContext.valid_from`/`valid_until`, is unrelated permit validity). **New Open Items:** six unresolved constitutional decisions blocking any GO-freshness implementation (validity interval/policy, mandatory-dependency definition, signed schema fields, enforcement ownership/transition mechanics, future reason codes, assurance-failure evidence schemas) — none invented, all left open; and the two Distinct Target-State Assurance Failures (telemetry unavailable vs. cryptographic assurance unknown) recorded as deliberately separate, neither implemented. `frontend/README.md` updated with a short cross-reference to this doctrine and the "TARGET STATE / NOT IMPLEMENTED" fixture-labelling convention for future work. No conflicts found between this task's instructions and repository ground truth.
- 2026-08-01 — Two work packages, documentation/verification only, no production code changed. **Package A (Multi-Agent Operating Model):** recorded Role Separation (Copilot drafts contracts/reconciles architecture; Gemini independently reviews UX/accessibility/field ergonomics/edge cases; Claude chat does conversational synthesis/continuity; Claude Code does repository inspection/scoped implementation/testing/reporting; human authority resolves constitutional questions before dependent implementation — prompts govern collaboration only, Core still adjudicates runtime authority) and the Structured Handoff Schema (`authority`/`task`/`facts`/`approved_contract`/`unresolved`/`excluded_scope`/`acceptance_tests`/`report`) as new, additive doctrine. **Deliberately omitted** an unverified technical claim about Gemini CLI/Claude Code isolated-context support, per instruction not to record it as fact. **Gate order NOT recorded — conflict found and reported, not resolved by guessing:** the task instructed recording a proposed 7-stage gate order as explicitly distinct from, and not superseding, "the 4-stage version recorded earlier today." No such 4-stage gate-order entry exists anywhere in this file — not in Locked Design Principles, not in Open Items, not in any prior changelog entry (checked the full file and `git log`; nothing resembling "Copilot defines → Gemini critiques → Copilot reconciles → Claude Code implements" appears anywhere in this repository's history). Per standing practice this session (verify before acting; stop and report rather than reconcile or guess when a prompt conflicts with ground truth), neither the 4-stage nor the 7-stage sequence was recorded. Awaiting clarification: was the 4-stage entry meant for a different session, a different repo, or is "recorded earlier today" itself mistaken? **Package B (NO_GO remediation verification):** re-verified the independent NO_GO remediation committed and pushed last session (`db958ef`) against its own acceptance tests — no re-implementation. All confirmed still compliant: "Do not proceed." primary instruction present for NO_GO; `role="alert"`/`aria-live="assertive"` fire only on a genuine dynamic transition into NO_GO, never on first render or for GO (confirmed via the 12-test browser harness, re-run live: 12/12 passing, no console errors); `BIND-999` still renders as non-interactive text, no `<a>`/`<button>`; full `rule_trace` and escalation block still render unconditionally; no freshness, countdown, or target-state assurance markup present anywhere in `blocked-screen.js` (confirmed by re-reading the full file against the diff already in `git log`). Full backend suite re-run: 92/92 passing. `git status` confirmed clean before and after this pass — no drift since last session. Nothing committed or pushed this pass, per instruction.
- 2026-08-01 — Recorded the Synapse UI Workflow Gate Order (Locked, 1 Aug) in the Multi-Agent Operating Model section, appended after Role Separation and Structured Handoff Schema: Copilot defines/normalizes the contract → Gemini critiques usability/clarity/accessibility/field conditions/edge cases → Copilot reconciles against Synapse doctrine → Claude Code inspects/implements/tests/reports. Closes the gap flagged in the previous session's report (the 4-stage sequence had been locked in conversation but never written to the repo). The separate 7-stage proposal remains undecided and out of scope — not recorded as locked, noted only as a still-pending alternative.
- 2026-08-01 — Locked the Two-Track Operational Readiness Model as a new Locked Design Principle: Platform Completion (Track A, bounded UI/software work) and Operational Readiness (Track B, real-world deployment work) are reported separately, never as one conflated percentage; Track B is formally ring-fenced behind the six unresolved GO-freshness constitutional decisions plus Willy's explicit go-ahead, and is explicitly identified as what the BCA Innovation Sandbox at the Built Environment Innovation Hub targets, so Track A demonstration work is never mistaken for Sandbox-ready readiness. No existing single-completion-percentage language was found anywhere in `CLAUDE.md` to replace. Documentation only — no production code changed, nothing committed or pushed this pass.
- 2026-08-01 — Recorded "Oversight and Support Roles" (Locked, 1 Aug) under Multi-Agent Operating Model, placed after Structured Handoff Schema per instruction. **Conflict found and flagged, not guessed around:** the requested anchor, "immediately after the Execution Evidence Authority subsection," does not exist anywhere in this file — that subsection was recorded only in a separate, non-canonical CLAUDE.md at `G:\My Drive\Synapse GDrive` during the same chat session, never in this repository. Stopped and asked before placing; Willy chose "after Structured Handoff Schema" over porting Execution Evidence Authority in. Recorded text is verbatim as supplied and still cross-references "(see Execution Evidence Authority, above)" — that reference does not resolve to anything in this file and is left as-is, unresolved, per instruction to record the text as given rather than silently edit it. Canonical-source check for this task: confirmed operating in `C:\Users\USER\dev\synapse`, HEAD `4a7f541fa896c30f152fb72efee761a242ce6ce0`, branch up to date with `origin/master`, working tree clean apart from one pre-existing unrelated untracked file. `G:\My Drive\Synapse GDrive` and OneDrive paths were not opened for this task, per instruction. Documentation only — no production code changed, nothing committed or pushed this pass.
- 2026-08-01 — Ported "Execution Evidence Authority" (Locked, 1 Aug) into this canonical CLAUDE.md, placed directly before Oversight and Support Roles, closing the gap from the previous entry above: the principle had only ever been recorded in the non-canonical `G:\My Drive\Synapse GDrive` copy (written there by an earlier session that didn't verify its working directory), never in this repository. The dangling "(see Execution Evidence Authority, above)" cross-reference inside Oversight and Support Roles now resolves correctly, since a real subsection by that name precedes it in this same file. Canonical-source check: confirmed operating in `C:\Users\USER\dev\synapse`, HEAD `4a7f541fa896c30f152fb72efee761a242ce6ce0`, branch up to date with `origin/master`, working tree otherwise clean apart from one pre-existing unrelated untracked file. `G:\My Drive\Synapse GDrive` and OneDrive paths were not opened for this task. Documentation only — no production code changed, nothing committed or pushed this pass.
- 2026-08-05 — Recorded "Escalation vs. Override — Decoupling Principle" (Locked, 5 Aug 2026) under Locked Design Principles, placed directly after the Reason Code Convention bullet and immediately before Communications Doctrine — adjacent to the existing Escalation Requirement (line 13) per instruction, following this file's established `###` sub-doctrine format (matching Communications Doctrine/GO Freshness Principle) rather than a new top-level `##` section, since the latter would have made it a sibling of "Locked Design Principles" itself rather than a member of it; content otherwise recorded verbatim as supplied. States that Frontline Worker escalation and Supervisor override are separate concepts not to be conflated in the Frontline Worker UI; notes `escalation_contact` on `OutboundAlert` correctly continues to resolve to `/supervisor/override` for Maestro's existing WhatsApp/Telegram adapters, but must not be reused as-is by a future Frontline Worker view ahead of the Supervisor Override Scope Open Item being resolved; sets v0.1 guidance that a Frontline Worker screen shows a contact/escalation path only, with no override button, language, or URL exposure, and that `assigned_role` is already structurally separable from `escalation_contact` if/when that surface is built. Documentation only — no production code or tests changed; 92/92 unaffected. Nothing committed or pushed this pass, per instruction to confirm the diff first.
- 2026-08-05 — **Belatedly logged here; commit `c65c394` at the time.** Built the first Frontline Worker screen: `src/frontline/router.py`'s `GET /frontline/blocked/{claim_id}` and `frontend/frontline-screen/`'s `<frontline-screen>` component. Renders for GO and NO_GO alike (no 409 on GO, unlike the Supervisor Blocked Screen) — caught during implementation that the originally-approved plan's "409 if not NO_GO" would have made the screen unable to ever answer "yes, proceed," which contradicts the Frontline persona's own question. Resolves a real `assigned_role` via `resolve_authority()` (the Supervisor Blocked Screen route deliberately doesn't). No rule internals, no override form/URL/language, by construction — enforces the Escalation vs. Override Decoupling Principle above. Live-verified in an actual browser (shadow DOM inspected directly), not just `TestClient`. Full suite: 96/96 passing (92 existing + 4 new).
- 2026-08-05 — **Belatedly logged here; commit `eb2e5c1` at the time.** Accessibility pass on `frontend/frontline-screen/`, per Gemini design-governance review (relayed by Willy). Computed actual WCAG contrast ratios rather than assuming: found and fixed one real failure (`.trace-id`'s `#777` measured 3.92:1/4.03:1 against the two screen backgrounds, below the 4.5:1 AA floor; darkened to `#666`, 5.02:1/5.17:1). Added a viewport meta tag (previously missing entirely) to both `demo.html` and the live route's rendered page, with scaling left enabled; reflow verified live at 320px width, no horizontal scroll. **Declined** the review's request for `aria-live="assertive"` on NO_GO: conflicts with this file's own Stage 2 Frontline Worker Contract ("role=alert used only... not as a default") and `blocked-screen.js`'s established reasoning (assertive semantics are for an already-rendered screen changing state; `<frontline-screen>` sets data once, server-side, on page load — there is no such transition). Kept `role="status" aria-live="polite"`. Full suite: 96/96 passing.
- 2026-08-05 — **Supervisor Override Retirement**, recorded as a new Locked Design Principle (above), superseding the Supervisor Override Scope Open Item and partially superseding the same day's Escalation vs. Override Decoupling Principle (kept verbatim for history, not edited — see that section's own superseding note). Handoff arrived via Willy relaying a Copilot/Gemini-authored proposal citing an existing "no appeals" principle and Singapore statutory grounding (Building Control Act / WSH Construction Regulations / MOM); **verified before acting per this file's Execution Evidence Authority:** no "appeal"/"appeals" text exists anywhere in this file — that continuity claim did not check out, flagged to Willy before proceeding, and the statutory claims themselves are outside anything git/filesystem access can verify, so they are recorded here as asserted grounding, not repo-verified fact. Also flagged and confirmed with Willy before proceeding: the requested `src/maestro/directory.py` restructuring is not override-scoped as described — `resolve_authority()`/`DIRECTORY_MAP` is shared by the override alert, every NO_GO alert (`src/airlock/router.py`), and the Frontline Worker screen, so the actual blast radius also touched `src/maestro/schemas.py` and `src/maestro/formatting.py`. Implementation: `POST /supervisor/override` returns `410 Gone` unconditionally (malformed bodies still 422 at the schema boundary first); `evaluate_override()`/`OverrideRecord`/`OverrideAuditEntry`/`persist_override_record()` left intact, not deleted, simply no longer called; `AuthorityBinding` gained an optional `role_type: AuthorityRoleType` field (new enum: `PE`/`QP`/`PI`/`PA`/`PM`/`SA`, `PR` deliberately excluded) — schema/structure only, no real PEB/MOM registration data, `DIRECTORY_MAP`'s untyped catch-all deliberately retained (removing it would fail closed on every Maestro alert and the Frontline screen, a larger decision not made here); `escalation_contact` no longer builds a `/supervisor/override` link, now states the resolved authority directly (`"General Duty Officer (BIND-999)"`); `render_alert_text()` dropped "escalate/override" phrasing. `frontend/blocked-screen/blocked-screen.js`'s override form is untouched and now surfaces the 410 message via its existing error-handling path — flagged as a new Open Item, not fixed here (out of this pass's explicit scope). Five new/updated Open Items recorded: PE/QP/PI/PA/PM/SA-to-registration binding, `reason_code`→role_type routing table, `ClaimPayload` authority-role identification, formal removal of retired override code, and the blocked-screen.js override form. Full suite: 99/99 passing (96 existing + 3 new `AuthorityRoleType` tests), all pre-existing override/escalation-contact assertions updated to match, none deleted outright.
- 2026-08-06 — **`R-AUTH-01` disambiguation — findings only, nothing implemented.** Read `check_authority()` (`src/core/rules.py`) and `adjudicate()` (`src/core/evaluator.py`) directly. Finding: the function receives the full claim (so `work_type`/`zone_id`/`action_type` are technically reachable), but its logic has exactly two branches — unauthenticated issuer, or `authority_level < clearance_level` — and neither is conditioned on claim domain; both map to the same `R-AUTH-01`. Confirmed: the check is currently too coarse to disambiguate structural/QP-plan/safety-adjacent failures; splitting it needs new branching logic in `check_authority()` itself, not just a downstream routing-table change. `rules.py`/`evaluator.py` left untouched this pass, per instruction.
- 2026-08-06 — **`ClaimPayload` authority-field options — findings only, nothing implemented.** Confirmed `authority_level` (int) is a generic clearance tier, compared only numerically against `IssuerRecord.clearance_level` — not role-like. Confirmed `AuthorizedIssuer.role` (str, free text) exists but is never read by `check_authority()` — persisted, unused by logic. Presented three non-recommended options for attaching a submitting role to a claim (new optional `ClaimPayload` field; extend/constrain the existing `role` column; separate binding table) with trade-offs, no implementation — see that round's own report for detail, not reproduced here.
- 2026-08-06 — **IssuerRole migration — implemented (Task 1 of this date's handoff); Tasks 2 and 3 findings/confirmation only, per instruction.** Real-world precedent (a named individual in the Synapse review process holds multiple independently-accredited statutory roles) corrected Option B's original single-`role`-per-issuer scope: an issuer can hold several roles, each independently checkable — no "verified once, trusted for everything." Implemented as a new join table, `IssuerRole` (`src/core/models.py`, table `issuer_roles`): one row per `(issuer_id, role_type)`, `issuer_id` a real `ForeignKey` to `authorized_issuers.issuer_id`, `role_type: AuthorityRoleType`. `AuthorizedIssuer.role` deliberately left untouched — confirmed by inspection it's still unread by `check_authority()` — rather than migrated in place, since forcing either of the only two real values in this repo (`"SUPERINTENDENT"`, `"SITE_ENGINEER"`, both generic job titles, neither a verified licensure claim) onto `AuthorityRoleType` would fabricate a credential with no basis. No real role rows seeded — same no-fabricated-data discipline as `DIRECTORY_MAP`'s catch-all-only policy. Schema doesn't preclude a future per-role `verified`/`registration_id` column (would live naturally per role-row, no restructuring needed), but none added now, per instruction. **Refactor caught and flagged, not silently done:** `AuthorityRoleType` moved from `src/maestro/directory.py` to a new `src/core/roles.py` — `IssuerRole` needed the same enum, and importing it from Maestro into Core would have inverted the "Core decides, Maestro delivers" dependency direction (a downstream layer's module ending up depended on by the foundational one); `directory.py` now re-exports it from `core/roles.py` for full backward compatibility, same enum object, no behavior change (`tests/test_core_repository.py::test_authority_role_type_canonical_import_is_core_roles` asserts identity, not just equality). Added `fetch_issuer_roles()` (`src/core/repository.py`) as read-access infrastructure only — genuinely queryable/testable, but **not called from `check_authority()`/`adjudicate()`/`IssuerRecord` anywhere** — wiring multi-role data into adjudication is explicitly the separate, not-yet-decided task Task 2 asked about, not solved as a side effect here (verified via `git diff` that `rules.py`/`evaluator.py`/`airlock/schemas.py` are byte-for-byte untouched this pass). Task 2 finding: since `check_authority()` doesn't read role at all today, the multi-role schema by itself doesn't force any change to it — but it sharpens the existing gap (no field on `ClaimPayload` says which of an issuer's several roles a claim is submitted under), which remains exactly the three-option, not-yet-decided question from the prior round. Task 3 confirmed: `R-AUTH-01` disambiguation remains a distinct, unstarted follow-on, not accidentally solved by this migration. Full suite: 103/103 passing (99 existing + 4 new), including a real `create_all()` sanity check against a live (in-memory) engine, not just model-definition inspection.
- 2026-08-06 — **`R-AUTH-01` disambiguation, implemented (Task 1 of this date's handoff).** New `classify_authority_failure()` (`src/core/rules.py`) is the single source of truth for which Rule 1 failure mode applies; `check_authority()` calls it to build `RuleOutcome.reason`, `adjudicate()` (`src/core/evaluator.py`) calls it a second time for `Verdict.reason_code` — a deliberate duplicate call to one pure function, not two copies of the branching logic, same pattern as `resolve_authority()`'s two call sites. New codes, honestly grounded rather than following the earlier speculative 5-way draft: `R-AUTH-01` stays unauthenticated-issuer-only (no claim-content signal can honestly distinguish sub-types of "we don't recognize this issuer" — it's identity-based, not domain-based); `R-AUTH-02`/`R-AUTH-03` split insufficient-clearance by the existing `HIGH_RISK_WORK_TYPES` distinction `verify_ptw_precondition` already uses — not a new taxonomy, the one real categorization already in this codebase. `claim.zone_id`/`action_type` deliberately not used: zone hazard level isn't available inside `check_authority()` (it never receives `zone_record`), and `action_type` has no established vocabulary to categorize against without inventing string-matching heuristics. This is a documented, deliberate exception to the Reason Code Convention's "one code per rule" (see that entry, amended above) — `RuleOutcome`/`rule_trace`'s wire shape is unaffected (`reason_code` stays `Verdict`-level, not added to `RuleOutcome`, so `src/maestro/schemas.py`'s `RuleConditionResult`, `extra="forbid"`, needed no change — checked this specifically before implementing, since adding a field to the shared `RuleOutcome` dataclass would have broken every NO_GO Maestro alert's construction). One existing test updated (insufficient-clearance-on-`NOMINAL_CIVIL` case: `R-AUTH-01` → `R-AUTH-03`, since that code now means something narrower); one new test added for the `R-AUTH-02` (high-risk work) case, previously uncovered. Full suite: 104/104 passing.
- 2026-08-06 — **Task 2 (`AuthorizedIssuer.role` → `List[AuthorityRoleType]`) — not implemented; flagged a conflict with already-shipped work instead of proceeding.** The handoff's literal instruction — replace `AuthorizedIssuer.role` with a `roles: List[AuthorityRoleType]` field — was not carried out, because it re-describes, without apparent awareness of it, the exact problem the `IssuerRole` join table (above, same date, earlier commit `47ec717`) was already built and shipped to solve: multiple independently-checkable roles per issuer. Implementing a `List[AuthorityRoleType]` column now would mean either discarding that already-tested, already-approved table, or shipping two parallel, duplicate mechanisms for the same fact. A list/array column is also the structurally worse fit for "each role independently checkable, no verified-for-one-trusted-for-all shortcut" than a join table is — a future per-role `verified` flag lives naturally on a table row; it does not live naturally inside one element of an array column without further nested structure. Flagged to Willy rather than silently reinterpreting the instruction or silently complying and overwriting recent work; Task 3 (reason-code-to-role routing) depends on Task 2 being "real" per the handoff's own sequencing, so it was not started either. Nothing changed in this entry beyond documentation-adjacent findings already covered by Task 1, above.
- 2026-08-06 — **Task 2 marked resolved, per Willy's explicit direction: `IssuerRole` (commit `47ec717`, above) is confirmed as the mechanism**, not a `List[AuthorityRoleType]` column on `AuthorizedIssuer`. No new code — the table already existed and already does what Task 2 asked for (multiple independently-checkable roles per issuer); this entry exists so the resolution itself is on record, not just the earlier flag.
- 2026-08-06 — **Task 3 (reason_code → role_type routing) — implemented, on top of Task 2's resolution.** Reuses the existing `("*", reason_code)` precedence tier in `src/maestro/directory.py`'s `DIRECTORY_MAP` — not a separate table, since this is the same `(zone_id, reason_code)` lookup `resolve_authority()` already does, keyed generically on `reason_code` rather than a specific zone. Only `R-ZONE-01` → `SA` is encoded (`BIND-SA-01`, `role="SA"` — left as the bare code, not an invented English expansion, since no confirmed definition of what "SA" stands for has been supplied anywhere). `BIND-999` remains the fallback for everything else, per instruction — `R-PTW-01`, `R-AUTH-01`, `R-AUTH-02`, and `R-AUTH-03` are deliberately left unrouted (see the Open Items entry, above, for exactly why each one — not a placeholder to fill in casually, a documented decision not to guess). Two existing tests updated (`R-ZONE-01` scenarios now resolve to `BIND-SA-01`/`"SA"` instead of the catch-all), two new `test_maestro_directory.py` tests added confirming both the routed and deliberately-unrouted cases explicitly. Full suite: 105/105 passing.
- 2026-08-06 — **Task A (role label resolution) — implemented.** New `ROLE_TYPE_LABELS`/`role_type_label()` (`src/core/roles.py`), deliberately incomplete: only `PE` ("Professional Engineer") and `QP` ("Qualified Person") are confidently known — `PE` matches the already-named PEB registration body, `QP` was explicitly named as a defined Building Control Act term in the handoff that introduced this authority set, neither inferred. `PI`/`PA`/`PM`/`SA` deliberately absent — "Safety Assessor" was used as an illustrative example in the Task A instructions requesting this map, not a confirmed source, and was **not** entered as fact; all four fall back to their bare code, which is what every screen already showed before this change (a genuine upgrade only for the two confirmed codes, not a cosmetic relabeling of everything). `src/maestro/directory.py`'s `R-ZONE-01` binding now computes its label via `role_type_label(AuthorityRoleType.SA)` instead of a hardcoded literal — behaviorally identical today (SA unconfirmed), but self-updating the moment a real SA label is confirmed. **The more serious gap, fixed:** `GET /supervisor/blocked/{claim_id}` (`src/supervisor/router.py`) previously never called `resolve_authority()` at all ("out of scope," by original design) — the screen meant for the person expected to act showed no role information whatsoever, just a bare binding ID. It now calls `resolve_authority(zone_id, reason_code)`, same pattern `src/frontline/router.py` already used, and populates `assignedRole`. Live-verified via `TestClient` on a real `R-ZONE-01` case, not just asserted: before this change the JSON hand-off had no `assignedRole` key at all; after, `"assignedRole": "SA"` appears, matching what the Frontline screen already showed. `escalationContact` (`"Your site supervisor"`) noticed as a separate, still-hardcoded placeholder while making this change — not fixed, flagged, out of scope for this pass; do not confuse it with `assignedRole`, which is now real. `frontend/README.md` and `blocked-screen.js`'s own doc comment updated to match (historical changelog entries above, dated 2026-07-31/2026-08-05, left as-is — accurate as of when they were written, per this file's own convention of not silently rewriting history). Full suite: 109/109 passing.
- 2026-08-09 — **`clearance_level` scale — findings only, nothing implemented; extends the 2026-08-06 `ClaimPayload` authority-field entry, above.** Investigated in support of the new `src/intake/` ePTW claim-source adapter (same-date work, see commit `f943711`), whose `authority_level` crosswalk is blocked pending a real external vendor's role vocabulary. This pass checked the *target* side — Synapse's own `clearance_level` — and found it equally undefined, not just vendor-unconfirmed. `AuthorizedIssuer.clearance_level` (`src/core/models.py:57`) is a plain, unconstrained `int` — no `CHECK` constraint, no `SAEnum`, no documented scale. `check_authority()`'s `claim.authority_level < issuer_record.clearance_level` (`src/core/rules.py:115`) is the only place it is ever compared — a raw `int < int`, with no role name, no `AuthorityRoleType` (PE/QP/PI/PA/PM/SA), and no bounds-checking on either side (`ClaimPayload.authority_level`, `src/airlock/schemas.py:93`, has no `field_validator`, `ge=`, or `le=`). Confirmed `AuthorityRoleType` and `clearance_level` have never been connected anywhere in this codebase — consistent with, and not contradicted by, the still-open "PE/QP/PI/PA/PM/SA authority set not bound to real registration data" Open Item, above. Every `clearance_level`/`authority_level` value in the repository was enumerated: only `0`, `1`, and `3` appear anywhere (`scripts/seed_dev_data.py:44`: `SUPERINTENDENT`=3, the only row ever seeded into a real dev database; `synapse_mdm.py:31-34`, the uncommented reference harness: `SUPERINTENDENT`=3, `SITE_ENGINEER`=1; test fixtures across `tests/test_adjudication.py`, `test_core_eptw.py`, `test_maestro_schemas.py`, `test_airlock_maestro.py`, `test_supervisor_logic.py`, `test_supervisor_router.py` reuse the same two role/level pairs; `authority_level=0` appears only in `tests/test_adjudication.py:84,100`, deliberately constructing an insufficient-clearance failure). `3 > 1` for `SUPERINTENDENT` vs. `SITE_ENGINEER` reads as "higher = more senior," but that is an inference from two data points and two job-title strings, not a documented policy — nothing explains the skipped value `2`, nothing caps a maximum, nothing floors a minimum. **Conclusion: no `clearance_level` scale exists to build a future role-name-to-integer crosswalk against.** Whoever eventually maps a confirmed external vendor's role vocabulary onto `clearance_level` will need a real clearance-level policy decision first, not just the vendor's side of the mapping — not designed here, no timeline. Documentation only — no production code or tests changed this pass; `src/intake/` and the identity crosswalk registry (`src/intake/models.py`, `src/intake/repository.py`) are untouched. Full suite unaffected: 128/128 passing (unchanged from commit `f943711`).
- 2026-08-11 — **Certified Profile module, implemented — new `src/profiles/` plus `src/core/profile_resolution.py`, commit `52688ea`.** `schemas.py`'s `CertifiedProfile`/`BaseProfileRef`/`ProfileLineage` (`STANDALONE` / `BASE_ANNEX`, explicit `lineage` field validated against `base_ref` presence rather than inferred from it), `models.py`'s zero-row `certified_profiles` table (registered in `src/core/init_db.py`'s `create_all()` import list, same pattern as every other module's table), `repository.py`'s `fetch_certified_profile()` (I/O only, returns `None` on no match, same convention as `fetch_issuer_record`/`fetch_device_public_key`). `src/core/profile_resolution.py`'s `resolve_effective_parameters()` is pure — no I/O, only ever sees already-fetched `CertifiedProfile` records, same discipline as `src/core/rules.py` — and returns `profile.parameters` unchanged for `STANDALONE`, or `{**base.parameters, **profile.parameters}` for `BASE_ANNEX` after confirming the supplied base actually matches `base_ref`'s pinned `(base_profile_id, base_profile_version)`; raises `BaseProfileMissingError`/`BaseProfileMismatchError` (two distinct failure modes, not collapsed into one, mirroring `src/telemetry/trust.py`'s `DeviceNotRegisteredError`/`TelemetrySignatureInvalidError` split) rather than guessing. `base_profile_version` is pinned, not a floating "latest," so resolution stays deterministic. Explicitly and deliberately kept separate from the still-undesigned Multi-regulator ruleset architecture Open Item (LTA/BCA/MOM within one jurisdiction) — this is a different axis (one jurisdiction's own base+annex code layering vs. standalone code), not a substitute or a merge. Zero rows shipped — no real Eurocode part, national annex value, or Singapore profile parameter fabricated, same discipline as `DeviceRegistryEntry`/`IdentityCrosswalkEntry`. Not wired into `src/core/rules.py`/`evaluator.py`/`src/airlock/` anywhere; no endpoint exists. Full suite: 151/151 passing (139 existing + 12 new, reconciled directly against `pytest -v`/`--collect-only` output before committing, not assumed). See the new Open Items entry, above, for what remains undecided.
- 2026-08-18 — Authority set corrected: five named roles (PE, QP, PA, PM, SA) plus two additional codes (QE, RTO). PI removed — was 'Principal Investigator,' a category error from R&D context; PIs don't gate site work. Dedicated guard test added to prevent reintroduction. PA remains unrouted pending independent confirmation of per-project liability assignment.
- 2026-08-18 — Authority directory expanded: RTO confirmed as the go-to gate for continuous execution-stage compliance (confirmed by Ganesh and Ben Chin). RTO now the live default routing target for GO (reason_code=None) and for R-PTW-01, R-AUTH-01, R-AUTH-02, R-AUTH-03, replacing the General Duty Officer catch-all for these five cases. R-ZONE-01 → SA unaffected. GDO catch-all (BIND-999) remains live as fallback for any reason_code not explicitly routed. QP/QE remain structural-only (inert placeholders) — conditional dispatch on a design-alteration flag not yet implemented; requires a ClaimPayload schema decision, still open. PM and PA remain unrouted: PM's operational decisions pass through RTO's gate rather than routing independently; PA's per-project liability assignment is still unconfirmed.
- 2026-08-18 — resolve_authority() changed to return list[AuthorityBinding] instead of a single binding, since reason_code-routing (RTO/SA) and the design-alteration flag are orthogonal and can both apply to the same claim. QP and QE currently fire together, always, on every is_design_alteration=True claim — nothing in the repo distinguishes a QP-type alteration from a QE-type one. This is an unconfirmed placeholder default, not a domain decision — Willy/Ganesh/Ben Chin have not reviewed or confirmed it. Chosen deliberately as the safer fail-closed default (over-notify rather than under-notify) pending real input. OPEN ITEM: revisit if QP/QE ever need to be distinguished on a single alteration claim.
- 2026-08-27 — **Telemetry ingestion pathway, implemented; extended same-day with rejection evidence.** `src/telemetry/zone_write.py`'s new `write_sensor_zone_state()` is the first real caller of `src/telemetry/trust.py`'s `verify_telemetry()`. `src/core/rules.py` gained `SENSOR_ELIGIBLE_ZONE_FIELDS` (currently `{active_crane}`) and `sensor_zone_redis_key()`; `src/core/repository.py`'s `fetch_zone_record()` merges a verified-telemetry Redis hash with the existing human-declared one, sensor value authoritative when present, human declaration fallback-only — `scripts/seed_dev_data.py`'s write path unaffected for zones with no registered sensor. A successful write gets a dedicated evidence record (`emit_sensor_zone_state_evidence()`, persisted via new `SensorZoneStateAuditEntry`, `source="VERIFIED_TELEMETRY"`). `TelemetrySignatureInvalidError` gained its own reason code (`R-DEV-02`), distinct from `DeviceNotRegisteredError`'s existing `R-DEV-01`. **Same-day addendum:** a *rejected* write attempt (either exception) now also gets its own signed, persisted record — `emit_sensor_zone_rejection_evidence()`, a new `SensorZoneStateRejectionAuditEntry` table (kept distinct from the success-path table, not a shared status column — see that table's own docstring), `source="TELEMETRY_REJECTED"` (deliberately not `VERIFIED_TELEMETRY`), `reason_code` pulled out as its own column. Caller-facing exception behavior is unchanged. Full suite: 194/194 passing (185 original + 9 across both telemetry passes). Not done in this pass: any query/read surface for rejection records; whether any `ZoneRecord` field beyond `active_crane` is genuinely dual-input. `check_authority()`/`AuthorityRoleType` wiring and GO Freshness remain untouched, out of scope.
- 2026-08-27 — **Authority Admissibility: role-based gating for `authority_check`, implemented (R-AUTH-02/R-AUTH-03 only).** `check_authority()`/`classify_authority_failure()` (`src/core/rules.py`) now gate on `GATE_ADMISSIBLE_ROLES` membership (currently `RTO` for both `R-AUTH-02` and `R-AUTH-03`) instead of the unscaled `claim.authority_level < issuer_record.clearance_level` comparison — the first real read of `fetch_issuer_roles()`/`IssuerRole`, previously built and tested but unread. `adjudicate()` and `POST /airlock/claims` (`src/airlock/router.py`) both gained a threaded `issuer_roles` parameter/fetch. `authority_level`/`clearance_level` remain on their schemas, unread, per instruction not to delete them. **R-ZONE-01 and R-PTW-01 admissibility (SA, RTO respectively) is recorded in `GATE_ADMISSIBLE_ROLES` but explicitly NOT wired** — `check_zone_safety()`/`verify_ptw_precondition()` have no existing call path carrying issuer-role data, and inventing one was out of scope for this pass; see the handoff report's structural finding. Design-alteration (QP+QE) routing is unchanged — it remains escalation-only via `DIRECTORY_MAP`, not a Core gate. Test impact was broader than initially scoped: because `authority_check` gates entry to `zone_safety_check`, every GO-path and zone-safety test across `tests/test_adjudication.py`, `tests/test_core_eptw.py`, `tests/test_maestro_schemas.py`, and `tests/test_airlock_maestro.py` needed an explicit admissible-role fixture, not just the two R-AUTH-02/R-AUTH-03-specific tests; `test_airlock_maestro.py`'s shared `_StubSession` was extended to serve `fetch_issuer_roles`' query shape alongside `fetch_issuer_record`'s. Full suite: 195/195 passing (194 existing + 1 new, confirming an issuer holding multiple roles passes via intersection, not exact match). Not decided here, per instruction: whether one role may satisfy multiple gates concurrently (the implementation supports it naturally but this wasn't a locked policy question); whether/how R-ZONE-01/R-PTW-01 admissibility should eventually get a real call path.
